CREATE TABLE  productcard(
id INTEGER NOT NULL PRIMARY KEY,
image TEXT,
name TEXT,
desc TEXT
);

insert:
INSERT INTO productcard(id,image,name,desc)
VALUES (?,?,?,?);

getProducts:
SELECT * FROM productcard;

delete:
DELETE FROM productcard WHERE id=:id;